package PlayableCharacter;

import Weapon.WeaponBehavior;

public class Champion extends Hero {
  public Champion(int x, int y, int roomNumber, WeaponBehavior weaponBehavior) {
    super(x, y, roomNumber, weaponBehavior);
  }
}